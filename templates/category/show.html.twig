{# templates/category/show.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}Categorie #{{ category.id }}{% endblock %}

{% block body %}
<div class="media">


  <div class="media-body">
    <h1 class="mt-0">{{ category.name }}  Last programs :</h1>
    {% for program in programs %}
      <p>{{program.id}} -  <a href="{{ path('program_show', { program: program.id }) }}">{{ program.title }} </a> 
      </p>
      <p>{{program.summary}}</p>
      
    {% endfor %}

  </div>

</div>

<a href="{{ path('category_index') }}">
    Back
</a>

{% endblock %}
